# pip install -r requirements.txt
# torch ships as a CUDA 12.8 wheel, so install it separately first:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

torch==2.11.0
transformers==5.14.1
accelerate==1.14.0
safetensors==0.8.0
datasets==5.0.0
huggingface_hub==1.25.1

# The reference REAP implementation. compressed-tensors comes with it and
# carries the offload machinery the scoring pass runs under.
llmcompressor>=0.9.0
compressed-tensors>=0.17.2

# Not used directly. transformers' patch machinery walks `dir(transformers)`,
# which imports every model module including aria's image processor, and that
# one imports torchvision at module scope. Without it, loading a model under
# `load_quantizable_moe` dies on ModuleNotFoundError. llm-compressor lists it
# as a dependency for the same reason.
torchvision==0.26.0
